<Window x:Class="Countdown.App.Views.CustomDurationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="自定义时长"
        Icon="/Assets/app.ico"
        Width="280"
        Height="150"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FF202020"
        Foreground="White"
        UseLayoutRounding="True">
    <StackPanel Margin="16">
        <TextBlock Text="输入时长（如 25、5m、90s、1m30s）"
                   Foreground="#FFCCCCCC"
                   FontSize="12" />
        <TextBox x:Name="InputBox"
                 Margin="0,8,0,4"
                 FontSize="16"
                 Padding="6,4"
                 InputMethod.IsInputMethodEnabled="False"
                 TextChanged="OnInputChanged" />
        <TextBlock x:Name="HintText"
                   Text="纯数字按分钟；可带 m / s 后缀"
                   Foreground="#FF9A9A9A"
                   FontSize="11" />
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button x:Name="OkButton"
                    Content="开始"
                    Width="72"
                    Margin="0,0,8,0"
                    IsEnabled="False"
                    IsDefault="True"
                    Click="OnOk" />
            <Button Content="取消"
                    Width="72"
                    IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
